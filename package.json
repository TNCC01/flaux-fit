{
  "name": "flaux-fit",
  "private": true,
  "description": "FIT, the Flaux workout timer. Static files, no build step; the only dependency is for the browser suite.",
  "scripts": {
    "check": "node scripts/selfcheck.mjs",
    "e2e": "node scripts/e2e.mjs",
    "test": "npm run check && npm run e2e"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
